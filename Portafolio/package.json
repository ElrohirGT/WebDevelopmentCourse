{
  "name": "portfolio",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "yarn && vite",
    "build": "yarn && vite build",
    "preview": "yarn && vite preview"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  },
  "dependencies": {
    "reveal.js": "^5.1.0"
  }
}
